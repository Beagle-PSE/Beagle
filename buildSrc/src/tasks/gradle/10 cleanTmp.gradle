/**
 * Contains a task cleaning the build/tmp folder.
 *
 * @author Joshua Gleitze
 */
 
allprojects {
	task cleanTmp {
		description "Deletes the temporary folder used by tasks."
		group "Build"
		
		doLast {
			file("$buildDir/tmp").deleteDir()
		}
	}
}