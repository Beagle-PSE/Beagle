def base = file(".").toPath()

include 'Core'
include 'GUI'
include 'Documentation'
include 'Web Presence'

file("Prototypes").eachDir() { dir ->
	// get path in form of 'prototypes/ProjectName'
	include base.relativize(dir.toPath()).toString()
}
